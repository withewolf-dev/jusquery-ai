import { Router } from 'express';
import scrapeController from '../controllers/scrape.controller';

const router = Router();

// router.post('/', (req: Request, res: Response) => scrapeController.scrapeWebsite(req, res));

export default router;  